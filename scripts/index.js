import Vue from "vue/dist/vue.esm.browser.min.js"
import MainBody from "./components/main"

/**
 * Vue instance
 */
new Vue({
    el: "#app",

    components: {
        MainBody,
    },

    data: function () {
        return {};
    },
});
